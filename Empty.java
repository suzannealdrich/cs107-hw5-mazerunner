//---------------------------------------------------------------80 columns---|/* Empty class * ----------- * This is the class that specifies the behavior of empty objects. * Each empty does nothing, really. */public class Empty extends Square {        public Empty(Location loc)    {        super(loc);     }    public synchronized void draw()    {        Display.drawAtLocation("Empty", location);        super.draw();    }}