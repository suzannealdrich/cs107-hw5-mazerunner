//---------------------------------------------------------------80 columns---|/* Enemy class * ----------- * This is the class that specifies the behavior of enemy threads. * Each enemy delays before making a move, and the delay time is  * dependent on a move counter which is incremented when energy is acquired. */public class Enemy extends Creature {    protected int pauseTime;    protected int moveCountdown;    public Enemy(Square start)    {        super(start);        moveCountdown = 0;    }    protected void delay()    {        if (moveCountdown > 0) {            moveCountdown--;            pause(pauseTime / 4);        } else            pause(pauseTime);    }            protected void storeEnergy()    {        moveCountdown += 50;    }    }