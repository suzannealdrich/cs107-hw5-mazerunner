//---------------------------------------------------------------80 columns---|/* Freezer class * ------------- * This is the class that specifies the behavior of freezer objects. * Each freezer signals entering creatures to freeze. */public class Freezer extends Square {        public Freezer(Location loc)    {        super(loc);     }    public synchronized void draw()    {        Display.drawAtLocation("Freezer", location);        super.draw();    }    public void enterAction(Creature occupant)    {        super.enterAction(occupant);        occupant.freeze();    } }