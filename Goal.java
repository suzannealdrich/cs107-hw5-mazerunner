//---------------------------------------------------------------80 columns---|/* Goal class * ---------- * This is the class that specifies the behavior of goal objects. * Each goal signals an entering creature to take (win) the goal if possible. */public class Goal extends Square {        public Goal(Location loc)    {        super(loc);     }    public synchronized void draw()    {        Display.drawAtLocation("Goal", location);        super.draw();    }    public void enterAction(Creature occupant)    {        super.enterAction(occupant);        occupant.takeGoal();    }}