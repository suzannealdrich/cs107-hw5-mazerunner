//---------------------------------------------------------------80 columns---|/* Ladder class * ------------- * This is the class that specifies the behavior of ladder objects. * Each ladder provides support. */public class Ladder extends Square {        public Ladder(Location loc)    {        super(loc);     }    public synchronized void draw()    {        Display.drawAtLocation("Ladder", location);        super.draw();    }    public boolean providesSupport()    {        return true;    }}