//---------------------------------------------------------------80 columns---|/* Timer class * ----------- * This is the class that specifies the behavior of timer threads. * All it does is pause and then signal the calling brick to close. */public class Timer extends Thread {    private static int TimerTime = 3000;    private Brick limboBrick;    public Timer(Brick dugBrick)    {        super("Timer Thread");        limboBrick = dugBrick;    }    public void run()    {         pause(TimerTime);        limboBrick.close();    }    public void pause(int numMilliseconds)    {        try { sleep(numMilliseconds); }        catch (InterruptedException ie) {}    }}